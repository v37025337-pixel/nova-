# Запуск Nova после интеграции UCR 18

Запрос: «Запусти ядро». Выполнен один обычный цикл на актуальном main
`b1eaf9cfb9181506e6786be99832914bff1285c6`: Nova 0.9 / runtime v9 / UCR 18.0.
Начальное состояние — журнал `experience/ucr18-integration/journal.json.gz`.

Ядро самостоятельно выбрало цель `nodes_total → source_sha256`
(`auto-0ac9f276886e28d9476f6ec1`, наблюдения github.com), зафиксировало восемь
обучающих строк и критерии поиска. Последовательность:
`GOAL_FROZEN → SEARCH_FROZEN → WITHHOLD / OBSERVATION_SEARCH_EXHAUSTED`.

UCR создал 2 408 вариантов на трёх глубинах: 24, 336 и 2 048.
Точного соответствия обучающим данным нет. Штатный поиск и контроль на той же
выборке выполнили по 10 654 попытки, также без решения. Новые задачи,
наблюдения и свежие проверочные ответы в прогон не добавлялись.

Новых навыков: **0**. Активна **G22**; геном и 21 прежний навык сохранены.
Проверка прежних примеров: **192/192 PASS**. Предсказуемость и полезность
выбранной связи не доказаны. Успешный запуск не является доказательством
развития интеллекта или перехода к G23.

| Проверка | Результат |
| --- | --- |
| Источники runtime перед запуском | PASS, 22 файла ядра и 3 файла инструментов |
| Исходные 124 события | Сохранены без изменений |
| Новые события | 3, полный журнал — 127 событий |
| База SQLite и экспорт | Полностью совпадают; цепочка хешей проверена |
| Первая проверка в отдельном процессе | Тайм-аут 180 секунд; исходный runner завершился с кодом 1 |
| Та же проверка с лимитом 600 секунд | PASS, код 0, 219.99 секунды |

Тайм-аут не скрыт: `driver-result.json` хранит первый исход,
`replay-attempt.json` — параметры и результат повторной проверки,
`restart-stdout.json` — настоящий вывод команды verify. Код runtime,
критерии допуска и стандартный runner не менялись.

Итоговый head: `ce9ab36241e801e7c16fe2004b1f0fc91a4d4aa74146e684b44a8d554b0fab3a`.
Полное состояние для следующего запуска сохранено в `journal.json.gz`;
`summary.json` связывает протокол, действия, контроль, регрессию и проверку.
Прогон завершён; постоянный фоновый процесс не запускался.

Команда запуска:

```bash
python scripts/run_ucr_development.py --parent experience/ucr18-integration/journal.json.gz --output experience/ucr18-continuation-20260921
```

После тайм-аута выполнена команда с внешним лимитом 600 секунд:

```bash
python -m nova_core --state experience/ucr18-continuation-20260921/state.sqlite verify --expected-head ce9ab36241e801e7c16fe2004b1f0fc91a4d4aa74146e684b44a8d554b0fab3a
```
