# UCR 16 подключён; Nova остаётся на G22

Проверено подключение пользовательского UCR 16 к текущей Nova поверх
`main` `5c47fe7a0425f579cb4c75bc2fe76df2c8b565d5`.
Полный код reader сохранён вместе с исправлениями совместимости.
Автор интеграции и протокола — разработчик; решение дальнейшей задачи
принимала Nova своим обычным циклом.

| Проверка | Результат |
| --- | --- |
| Локальные тесты | 117/117 |
| Исходники ядра, прочитанные UCR | 21/21; точные байты, AST и замкнутые графы |
| Исторические входы/выходы, повторно исполненные Nova | 192/192 |
| Новые документы в журнале | 23: 21 исходник и два набора трасс |
| Гипотезы UCR с поддержкой его внутреннего разбиения | 1 из 21 навыка |
| Дальнейшие шаги Nova | Выбор цели → фиксация поиска → WITHHOLD |
| Новые операторские учебные задачи | 0 |
| Новые принятые поколения | 0 |
| Регрессия прежних навыков | 192/192, 21 навык |
| Воспроизведение отдельным процессом | PASS, 116 событий |

Поддержанная гипотеза — сложение двух числовых полей в навыке
`230-repository-counts`. Для двух навыков получены слабые гипотезы,
для остальных 18 объяснение не найдено. Набор примитивов UCR не покрывает
значительную часть строковых, JSON- и других действующих механизмов Nova.
Это проверка анализа уже известных примеров, не новая способность и не
независимое подтверждение обобщения.

Выбранная Nova цель — `key → datasetKey` из прежних наблюдений GBIF.
Источники её ошибок относятся к событиям до добавления UCR. Новые описания
поступили в память, но этот запуск не доказывает, что они улучшили выбор цели.
Причинная польза подключения для самостоятельного обучения пока не показана.
Текущий критерий выбора не устанавливает предсказуемость зависимости до поиска.
Поиск завершился `OBSERVATION_SEARCH_EXHAUSTED`; кандидата для admission нет.

Геном G22 сохранился:
`81721cc4fe5cd51b96114598b3c19b72f52f18f075065f7e0ee371381e81947d`.
Новый head журнала:
`d1db00260fae36429fcf38abf30d1c5a19504cb88e29c103037cd0512a0e6cb9`.

`summary.json` и `verification.json` содержат сводку. `source-analysis.json`
и `grammar.json` — результаты анализа исходников. `programs-*.json`,
`traces-*.json` и `cognitive-cycle-*.json` сохраняют привязку исторического
опыта и вывод UCR. `actions.json` содержит реальные решения ядра.
`journal.json.gz` — полный журнал для воспроизведения без сетевых запросов.
SQLite остаётся локальным рабочим состоянием и в Git не включается.

Повтор опыта:

```bash
python scripts/run_code_reader_cycle.py --source-commit 5c47fe7a0425f579cb4c75bc2fe76df2c8b565d5 --output /tmp/nova-ucr-rerun
```

Восстановление сохранённого результата:

```bash
python -c 'import gzip,pathlib; pathlib.Path("/tmp/nova-ucr-journal.json").write_bytes(gzip.decompress(pathlib.Path("experience/ucr-v16-integration/journal.json.gz").read_bytes()))'
python scripts/run_development_chain.py --restore /tmp/nova-ucr-journal.json --state /tmp/nova-ucr-restored.sqlite
```

Интеграция расширяет инструмент анализа Nova. Рост интеллекта, самостоятельная
перепись архитектуры и появление сознания этим опытом не установлены.
