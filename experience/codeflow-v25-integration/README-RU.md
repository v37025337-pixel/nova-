# UCR v25: импорт статических графов в Nova

Запрос пользователя: выполнить слияние и обновить Nova присланным файлом
`ucr_v25_real_flow_cores.json`. Импорт выполнен поверх последнего реального
прогона G22 из `experience/ucr18-continuation-20260921/journal.json.gz`.
Его 127 событий сохранены без изменений.

| Проект | Заявленная версия | Узлы | Связи |
| --- | --- | --- | --- |
| requests | 2.32.5 | 66 | 73 |
| urllib3 | 2.7.0 | 56 | 62 |
| Всего | — | 122 | 135 |

Исходный файл сохранён байт в байт в `experience/codeflow-v25-input/`.
SHA-256: `ecf01e81c7cd39b851e1da30bb86304a9610a9d384651fab89793927e3c73c36`.
Файл и импортёр закреплены до запуска в commit
`8814b83cf830935c119ac789a5fc262d6bd4e7d8`.

При прямой передаче всего JSON штатный парсер останавливается на 128 записях.
Импортёр создал четыре документа: узлы и связи каждого проекта отдельно.
Все 257 записей графа и четыре записи происхождения приняты без усечения.
У всех документов один URL исходного файла; части не изображают независимые
источники. Уникальность узлов, согласованность идентификаторов, отсутствие
потерянных концов и повторов связей проверены.

Результат: **PASS_IMPORT**. В журнал добавлены четыре события `observation`;
полная история содержит **131 событие**. Ядро сохраняет **G22**, тот же геном
и **21 навык**. Регрессия — **192/192 PASS**; новые навыки — **0**.
Этот запуск импортирует данные, шаги самостоятельного поиска не выполнялись.

Полное семантическое воспроизведение в отдельном процессе: **PASS**, код 0,
204.00 секунды при лимите 600 секунд. Экспорт совпадает
с рабочей SQLite-базой, цепочка хешей проверена. Итоговый head:
`513aa3bf5284ea4ca43543d204f077e09e2b0b1abde83780093a31420387c973`.

Локальные тесты: **135/135 PASS**, включая семь проверок нового импорта.
Реальные результаты сохранены в `summary.json`, `observations.json`,
`receipts.json`, `regression.json`, `restart-stdout.json`, `unit-tests.txt`
и `artifact-verification.json`. Файл `journal.json.gz` содержит полное
состояние для следующего запуска; его хеш и head закреплены в
`canonical/static-codeflow.json`.

Связи и версии библиотек получены из пользовательского файла. Они не проверены
по исходникам upstream или динамическому выполнению. Название проекции
`intermodule-causal-core` само по себе не подтверждает причинность.
Данные UCR v25 не заменяют исполняемый UCR 18.0 и не доказывают появление
нового навыка, рост интеллекта или переход к G23.
